@{
    ViewData["Title"] = "WellTest";
}
@model List<WellTest>

<div class="row">
    <div class="mb-2 text-center col-lg-12 @(ViewBag.StatusCode == 200 ? "bg-success" : "bg-danger")">
        <b>@(ViewBag.StatusCode == 200 ? "Connect" : "Not Connect") [@ViewBag.StatusCode] - with API Ocelot</b>
    </div>
    <div class="col-lg-6">
        <h1>WellTest</h1> <hr />
        @if (ViewBag.ShowTable)
        {
            <a class="btn btn-primary btn-sm" asp-controller="WellTest" asp-action="Create">Add New</a>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Date</th>
                        <th>Type</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var _WellTest in Model)
                    {
                        <tr>
                            <td>@_WellTest.Id</td>
                            <td>@_WellTest.Name</td>
                            <td>@_WellTest.Date.ToShortDateString()</td>
                            <td>@_WellTest.Bopd</td>
                            <td class="text-end">
                                <form asp-controller="WellTest" asp-action="Delete" method="post">
                                    <input type="hidden" name="id" value="@_WellTest.Id" />
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>